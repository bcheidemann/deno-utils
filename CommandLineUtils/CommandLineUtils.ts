import { assert, createAssertion } from "./assert/assert.ts";
import { getOption } from "./getOption/index.ts";
import { run } from "./run/index.ts";

export const CommandLineUtils = {
  assert,
  createAssertion,
  getOption,
  run,
};
